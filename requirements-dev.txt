# Development and CI dependencies (pinned versions)
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-asyncio>=1.3.0
pytest-cov>=5.0

# Linting & formatting
ruff>=0.8.0

# Type checking
pyright>=1.1.390

# Build (for install -e .)
build>=1.0
setuptools>=69
wheel>=0.44
