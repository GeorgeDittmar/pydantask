---
name: postgres-migration
description: Generates, validates, and runs PostgreSQL Alembic migrations safely. Use when creating DB tables, adding columns, or altering schema.
license: MIT
compatibility: postgresql >= 14, python >= 3.10
allowed-tools: bash python_exec git_status
metadata:
  version: "1.2.0"
  author: "data-team"
  tags: ["database", "python", "alembic"]
---